# Öğretmen paneli şifresi. Bu dosyayı .env olarak kopyalayıp değeri değiştirin.
# Render'da bu değer panelden (Environment) girilir, depoya YAZILMAZ.
ADMIN_PASSWORD=degistir

# İsteğe bağlı: yerel port (Render kendi PORT değerini verir)
# PORT=3000
